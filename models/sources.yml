version: 2

sources:
  - name: stripe
    database: raw
    schema: stripe
    tables:
      - name: payment
  - name: jaffle_shop
    database: raw
    schema: jaffle_shop
    tables:
        - name: orders
          freshness: 
            error_after:
              count: 1
              period: day
            warn_after:
              count: 12
              period: hour
          loaded_at_field: _ETL_LOADED_AT
        - name: customers